import { balloons } from './balloons.js'
describe('balloons', () => {
  it('case 1', () => {
    const input = 
    const result = balloons(input)
    result.should.to.equal()
  })
  it('case 2', () => {
    const input = 
    const result = balloons(input)
    result.should.to.equal()
  })
  it('case 3', () => {
    const input = 
    const result = balloons(input)
    result.should.to.equal()
  })
})
